export const translationConsoleSettings = {
   "tcSearchType": {
      default: [true, "Default token and phrase"],
      values: ["Default token and phrase", "Exact match", "Substring match"],
      description: "Search type: ",
      title: "This setting determines which search type setting to use for TC when right-clicking a string and using the context menu to open TC."
   }
};