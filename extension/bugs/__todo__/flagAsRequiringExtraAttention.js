export default function flagAsRequiringExtraAttention() {

  // Add a button to the transEditor, which when clicked, adds an icon next to the segment in question, and adds a small icon to a new toolbar. This new toolbar has buttons that can be clicked to go to the relevant segment.
  // The toolbar on top should have a 'remove all labels' right-click option, as well as the ability to remove individual labels
  // Moreover, the button in the transEditor can be toggled, of course, which also triggers a change in the top menu.
  //
  // Later, this can all be synced with the inbox, etc.
  //
  //

}
